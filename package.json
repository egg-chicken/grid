{
  "repository": {
    "type": "git",
    "url": "https://github.com/egg-chicken/grid.git"
  },
  "name": "grid",
  "version": "0.0.0",
  "license": "MIT",
  "main": "./out/index.js",
  "description": "this is test package",
  "scripts": {
    "lint": "coffeelint src",
    "test": "mocha --compilers coffee:coffee-script/register",
    "postinstall": "coffee -b -o out/ -c src/"
  },
  "dependencies": {
    "coffee-script": "^1.10.0"
  },
  "devDependencies": {
    "coffeelint": "^1.16.0",
    "mocha": "^3.4.2"
  }
}
